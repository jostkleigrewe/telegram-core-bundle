<form method="post" action="{{ path('app_telegram_log_webhook_backend_delete', {'id': telegram_log_webhook.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ telegram_log_webhook.id) }}">
    <button class="btn">Delete</button>
</form>
